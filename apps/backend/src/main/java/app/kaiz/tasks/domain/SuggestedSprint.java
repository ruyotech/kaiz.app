package app.kaiz.tasks.domain;

/**
 * Suggested sprint placement enumeration. Defines where tasks created from this template should be
 * placed by default.
 */
public enum SuggestedSprint {
  CURRENT,
  NEXT,
  BACKLOG
}
