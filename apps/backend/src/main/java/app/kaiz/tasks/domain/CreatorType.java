package app.kaiz.tasks.domain;

/**
 * Creator type enumeration. SYSTEM = Global admin-created templates visible to all users. USER =
 * User-created personal templates.
 */
public enum CreatorType {
  SYSTEM,
  USER
}
